source "https://rubygems.org"

# Matches the GitHub Pages build environment (actions/jekyll-build-pages
# uses this same gem). Run `bundle exec jekyll serve` to preview locally.
gem "github-pages", group: :jekyll_plugins
